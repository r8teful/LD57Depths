using UnityEngine;

// Right now we have one of these scripts attached to each of the buttons in the control panel UI, which sounds like a stupid idea but maybe not?
public class TrenchZone : MonoBehaviour {
    public ZoneSO ZoneData;
}